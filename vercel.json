{
  "version": 2,
  "buildCommand": "npm install --prefix backend && npm run build --prefix backend 2>/dev/null || true",
  "functions": {
    "api/**/*.js": {
      "memory": 512,
      "maxDuration": 60
    }
  }
}
